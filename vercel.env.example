# Vercel environment variables (EXAMPLE)
# DO NOT commit real secrets. Use the Vercel dashboard to set production/preview env vars.

# Public (anon) values the frontend expects
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Service role key (server-side only) - NEVER commit this file with a real key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional deployment helpers
# SUPABASE_USER_EMAIL=your-email@example.com
# SUPABASE_USER_PASSWORD=your-password

# Note:
# 1) If you find a file in your git history that exposes a service role key, rotate that key immediately via the Supabase dashboard.
# 2) Deleting files and committing does NOT remove them from git history — follow the repo-history purge steps in the project README or use git-filter-repo/BFG to scrub history.
